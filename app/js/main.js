$(function () {
  var mixer = mixitup('.products__inner-box');

  $('.slider__inner').slick({

    dots: false,
    infinite: true,
    speed: 500,
    // fade: true,
    // cssEase: 'linear'
  });
});